# DEMO 验证结果（任务 1–6，三国一回合）

> 模型 qwen3.5:9b Q4_K_M / 本机 M4 Pro 24GB / ollama 0.30.10。三国=ENG/FRA/GER。

## 6 项全跑通
| 任务 | 状态 | 数据 |
|---|---|---|
| 1 接引擎 | ✅ | S1901M→F1901M，非法命令剔除+hold，存档 OK |
| 2 接网关 | ✅ | 一次 chat 2.0s、格式 0 失败 |
| 3 AI下令 | ✅ | 非法 2/15、格式失败 9%、意图 5/5 有真目标 |
| 4 谈判并发 | ✅ | 3 国 asyncio.gather，max13s≈avg5.3s 的~3×内（并发有效） |
| 5 一回合闭环 | ✅ | 谈判5轮→下令(rej全空)→结算，墙钟 68.8s |
| 6 Debug落盘 | ✅ | 每调用 prompt/响应/延迟/token/重试 → logs/calls-*.jsonl，stats→summary.csv |

## 关键发现（决定后续走向）
1. **不能用 ollama `format=schema`**：qwen3.5:9b 上语法约束慢 35× 或挂死；`/v1` 不认 think 开关也慢 35×。→ 走原生 `/api/chat` + `think:false` + prompt 注入字段 + 宽松提取 JSON。
2. **延迟达标**：单次 2–9s，并发取最慢者，三国一回合 ~69s；7 国预计仍可接受，下令最贵(8.9s)。
3. **格式失败率**：意图/下令 0%，谈判 36%（Message schema 短板）→ 下一步重点。
4. **像活人**：意图“入侵德国/夺柏林”，下令多合法，3 国全合法结算。9b 够用，无需降档。

## 验收回答
- 一回合时延：69s/三国，可接受。
- 格式失败率：意图/下令 0–9%、谈判 36%，需收紧 Message。
- 9b 像有意图：是。
- 合并/降档：暂不降档；可考虑 intent 并入 order 省 1 调用。

## 阶段一：4b 复测 + 谈判格式四招（2026-06-20）
主力换 qwen3.5:4b。谈判精简 type/recipient/content + 死模板 + 宽松解析(补括号+全角引号归一) + retry1。
| 指标 | 9b | 4b |
|---|---|---|
| 单次 smoke | 2.0s | 3.3s |
| 单国下令延迟/失败 | 8.9s/0% | 1.6s/0% |
| 一回合墙钟 | 68.8s | 41.4s |
| 谈判失败率 | 36% | **12%**(修引号前 32%) |
| 意图/下令失败 | 0% | 0%/0% |
- **根因**：谈判失败几乎全是模型用全角引号 `”` 收尾 → JSON 挂；归一化后 5/6 救回。
- 4b 更快更省、仍像有意图；定为主力，谈判降至 12%(retry 后近 0 丢言)。intent 暂独立。

## 阶段二：P1–P9 模块（2026-06-20，23 单测全过）
P1记忆库·P2Agent五步·P3引擎·P4编排器·P5网关档位·P6联调7国·P7编年史·P8debug·P9web。
每模块=实现+单测+snapshot/日志。骨架全搭、接口联调通。

### 7 国实战验证
真跑 7 国 2 相不崩、全国推进；139 调用、avg11s/max30s、失败 24%（并发争 24GB 比三国 12% 高）→ 满场延迟为头号优化点。

### P9 Web 手测（CC 测不了浏览器，待晨验）
1. 起服：`uv run uvicorn diplomind.web:app --port 8731`（已验证端点起得来、返 JSON/HTML）。
2. 浏览器开 `http://localhost:8731/`，点「下一相」→ 7 国跑一相，中心数/编年史刷新（每相含 7 国≈调用，约 1–2 分钟）。
3. 上帝视角内脏：`GET /api/snapshot`（意图/记忆/暗盘全可见，供观战+debug）。
- 现为骨架：地图用文字中心数、聊天/下令面板待补 P9 二期；后端接口已通。

## 待办
- 收紧谈判 Message schema（占失败大头）；7 国满场延迟实测；态度评分+记忆兑现；冬季造兵；Web 前端。
