# 交接说明 — 给 Claude Code

> DEMO 1–6 已跑通（见 RESULTS.md）。当前任务：降级到 4b 复测 + 修谈判格式失败。

## 1. 降级模型重测
- 主力换 **qwen3.5:4b**（9b 跑得动但机器太热，弃用）。
- 重跑 DEMO 1–6，对比 9b：单次/一回合延迟、内存占用、温度、各阶段格式失败率。
- 仍走原生 `/api/chat`+`think:false`+宽松JSON提取，勿用 ollama format=schema。
- 上下文只喂摘要+近3回合防 KV 膨胀；intent 可并入 order 省一次调用。

## 2. 修谈判格式失败（9b 已 36%，4b 会更高）
AI 谈判要吐结构化消息，常吐歪（字段缺/recipient乱/JSON坏）。四招叠加压到 10% 内：
1. prompt 给死模板+范例，"只输出此 JSON，别加解释"。
2. 宽松解析兜底（json-repair 类，从文本抠 JSON、补括号）。
3. 字段精简：消息仅 `type/recipient/content`。
4. 解析失败重试1次，仍坏=该轮不发言(空轮)+记日志，不崩。

## 阶段一验收
- 4b 各阶段失败率（目标谈判≤10%）；延迟/内存/温度对比 9b；4b 是否仍像有意图。
- 据此定：4b 够用否、intent 是否并入 order。

## 阶段二：基本实现框架（含 web UI）
顺序：核心模块先做扎实，再往宽度，每模块搭能跑的骨架。
- 核心扎实：态度评分+承诺兑现+记忆兑现（AI 因背叛改态度，会记仇）；完整一回合→冬季造兵→多回合到胜负。
- 铺宽度：9 模块骨架全搭，消息总线/编排器/网关接口联调通。
- Web UI：地图+三态聊天+下令+编年史tab+观战/debug；CC 测不了 UI，做到代码完成+手测说明，待用户早上验。
- 规则：每模块单元测试必过，不绿不推进；接口联调通。最终效果用户确认。
- 模糊处取合理默认继续、记决策日志；一问题反复3次修不好→跳过并记录。

### 阶段二分解（按模块逐个：实现→完整单测→可观测→能联调即联调）
现状：网关/引擎/总线/编排器/Agent=demo 级可跑；记忆库17行空壳；编年史/前端/观战/debug 未做。
每模块统一交付：完整实现 + 单元测试过 + snapshot()+结构化日志 + 与已成模块接口联调。

P1 **记忆库 Memory（核心，最先）**：态度评分(模型评)、承诺账本(剩余/永久、到期失效)、行动记录(背叛戳)、diary、滚动摘要(只出摘要+近3回合)。测：背叛后信任降、承诺到期失效、长局上下文不膨胀。
P2 **Agent 五步完整**：感知/态度更新/意图/谈判/下令补全；接P1记忆兑现（谈判格式收紧已属阶段一）。测：意图延续、下令仅合法、背叛后态度变。
P3 **操作引擎补全**：撤退/造兵/判胜负(18中心/平局)、3-7人中立国、存档续。测：造兵=新增中心、跑通整年。
P4 **编排器完整年循环**：春→秋→冬全相、计时/轮次/最大回合。测：3国3回合不崩、静默提前止。
P5 **网关**：4b档+异质分配+格式失败计数,已近成,补可配。测：失败率/重试统计。
P6 **联调**：7国多回合到胜负贯通。测：意图/谈判/记忆/下令串通。
P7 **编年史**：纯文本按年+公开发言,分段。测：仅公开信息。
P8 **可观测/Debug 面板**：聚合各模块 snapshot+日志,按回合/国家/通道查。
P9 **Web UI**：地图+三态聊天+下令+编年史tab+观战/debug；CC 测不了→代码完成+手测说明。

可观测性非独立阶段，是每模块必交项(技术架构§9)：只读 snapshot、结构化日志、LLM全量落盘、引擎hold/error、实时状态。
